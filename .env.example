# ─── Required ────────────────────────────────────────────────────────────────

# PostgreSQL connection string
DATABASE_URL=postgresql://memory:your_database_password_here@localhost:5432/memory

# OpenAI API key (used for embeddings and LLM calls)
OPENAI_API_KEY=your_openai_api_key_here

# PostgreSQL password (used by Docker Compose to initialize the database)
DB_PASSWORD=your_database_password_here

# ─── Models & Embeddings ─────────────────────────────────────────────────────

# EMBEDDING_MODEL=text-embedding-3-small
# EXTRACT_MODEL=gpt-5-mini
# CONFLICT_MODEL=gpt-5-nano
# EMBED_DIM=1536

# ─── Search & Limits ─────────────────────────────────────────────────────────

# DEFAULT_SEARCH_LIMIT=10
# DEFAULT_LIST_LIMIT=50
# DUP_THRESHOLD=0.95
# CONFLICT_THRESHOLD=0.55
# RELATES_TO_THRESHOLD=0.65
# MIN_SECTION_LENGTH=100
# MAX_TAXONOMY_PATHS=40

# ─── OpenAI & Concurrency ────────────────────────────────────────────────────

# OPENAI_TIMEOUT_S=60
# OPENAI_MAX_RETRIES=5
# MAX_CONCURRENT_API_CALLS=5
# EXTRACT_REASONING=low
# CONFLICT_REASONING=minimal

# ─── Database Pool ───────────────────────────────────────────────────────────

# PG_POOL_MIN=1
# PG_POOL_MAX=10
# STAGING_RETENTION_DAYS=7

# ─── Server ──────────────────────────────────────────────────────────────────

# PRODUCTION_PORT=8766
# ADMIN_PORT=8767
# LOG_LEVEL=INFO
# MCP_TRANSPORT=streamable-http
# FASTMCP_JSON_RESPONSE=1

# ─── System Primer ───────────────────────────────────────────────────────────

# PRIMER_UPDATE_MAX_AGE_S=3600

# ─── Ingestion Limits ────────────────────────────────────────────────────────

# MAX_MEMORIZE_TEXT_LENGTH=500000

# ─── Context Store ───────────────────────────────────────────────────────────

# CONTEXT_DEFAULT_TTL_HOURS=24
# CONTEXT_MAX_VALUE_LENGTH=50000
# CONTEXT_MAX_KEY_LENGTH=200

# ─── Backup Service (optional) ───────────────────────────────────────────────
# Remove the memory-backup service from docker-compose.yml if you don't need backups.

# GitHub Personal Access Token with `repo` scope
# GITHUB_PAT=your_github_personal_access_token_here

# Target repository in owner/repo format
# GITHUB_BACKUP_REPO=your-org/your-memory-backup-repo

# Seconds between backup runs (default: 21600 = 6 hours)
# BACKUP_INTERVAL_SECONDS=21600
